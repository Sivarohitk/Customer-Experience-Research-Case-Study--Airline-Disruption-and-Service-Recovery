# Raw Data

Store original source files here.

The included `demo_airline_feedback.csv` is a small starter dataset so the pipeline can run end-to-end without downloading anything first.

Recommended schema:

- `review_id`
- `airline`
- `source`
- `review_date`
- `disruption_type`
- `feedback_text`
- `rating`
- `route`

